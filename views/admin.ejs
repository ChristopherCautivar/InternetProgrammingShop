<!DOCTYPE html>
<!-- From Lab 10, incomplete, need app.js calls, ctrl-f "admin" -->
<html>
    <head>
        <title> </title>
        
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
        <script>
        //     $(document).ready(function(){
        //         $(".removeAuthor").on("click", function(event){   
        //             if (confirm("Delete Author?") == false){
        //                 event.preventDefault(); //prevent default behavior of link
        //             }
        //         });
        //     }); //doc ready
            function deleteConfirm(){
                if (!confirm("Delete Product?")){
                        event.preventDefault(); //prevent default behavior of link
                    }
            }
        </script>
    </head>
    <body>
        <h1> Admin Section </h1>
        <form action="/logout">
        <button> Logout </button>
        <form action="/addProduct">
        <button> Add New Product </button>
        </form>
        <h3> Products</h3>
        <% productList.forEach(function(productList){ %>
            [<a href="/updateProduct?productID=<%= productList.productID %>">UPDATE</a>] 
            [<a onclick="deleteConfirm()" class="removeProduct" href="/deleteProduct?productID=<%= productList.productID %>">DELETE</a>] 
            <%= productList.productName %>  <%= productList.productID %> <br>
        <% }) %>
    </body>
</html>